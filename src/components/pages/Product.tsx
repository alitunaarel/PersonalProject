import React from "react";
import Button from "../button/Button";

const Product = () => {
  return (
    <div>
      <Button label='Button' onClick={() => null} />
    </div>
  );
};

export default Product;
